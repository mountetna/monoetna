// mock "metis.development.local/${project_name}/list" with {buckets: [{bucket_name:'data'}]}
// mocks for items:
// `metis.development.local/${project_name}/list/${bucket}/${folderPath}` with ...
// contents built in form:
// {
//  folders: [{folder_name: <name>}],
//  files: [{file_name: <name>}]
// }

